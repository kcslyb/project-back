<?xml version="1.0" encoding="UTF-8"?>

<configuration>
    <properties>
        <!-- 文件输出格式 -->
        <property name="dateTime">sys</property>
        <property name="CONSOLE-PATTERN">%highlight{%d{yyyy-MM-dd HH:mm:ss} [%-5level] %C : %msg%n}</property>
        <property name="ERROR-FILE">${dateTime}.log</property>
    </properties>

    <appenders>
        <Console name="CONSOLE" target="SYSTEM_OUT">
            <PatternLayout pattern="${CONSOLE-PATTERN}"/>
        </Console>

        <File name="FileAppender" fileName="${ERROR-FILE}">
            <PatternLayout pattern="${CONSOLE-PATTERN}"/>
        </File>

        <Async name="AsyncAppender">
            <AppenderRef ref="FileAppender"/>
        </Async>
    </appenders>

    <loggers>
        <logger name="cn.kcs.order.dao" level="DEBUG"/>
        <logger name="cn.kcs.note.dao" level="DEBUG"/>
        <root level="info">
            <appenderref ref="CONSOLE"/>
            <appenderref ref="FileAppender"/>
        </root>
    </loggers>

</configuration>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.kcs.note.dao.TRolePermissionDao">

    <resultMap type="cn.kcs.note.entity.TRolePermission" id="TRolePermissionMap">
        <result property="rolePermissionId" column="role_permission_id" jdbcType="VARCHAR"/>
        <result property="roleId" column="role_id" jdbcType="VARCHAR"/>
        <result property="permissionId" column="permission_id" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TRolePermissionMap">
        select
          role_permission_id, role_id, permission_id
        from myproject.t_role_permission
        where role_permission_id = #{rolePermissionId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TRolePermissionMap">
        select
          role_permission_id, role_id, permission_id
        from myproject.t_role_permission
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="TRolePermissionMap">
        select
        role_permission_id, role_id, permission_id
        from myproject.t_role_permission
        <where>
            <if test="rolePermissionId != null and rolePermissionId != ''">
                and role_permission_id = #{rolePermissionId}
            </if>
            <if test="roleId != null and roleId != ''">
                and role_id = #{roleId}
            </if>
            <if test="permissionId != null and permissionId != ''">
                and permission_id = #{permissionId}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="rolePermissionId" useGeneratedKeys="true">
        insert into myproject.t_role_permission(role_permission_id, role_id, permission_id)
        values (#{rolePermissionId}, #{roleId}, #{permissionId})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update myproject.t_role_permission
        <set>
            <if test="roleId != null and roleId != ''">
                role_id = #{roleId},
            </if>
            <if test="permissionId != null and permissionId != ''">
                permission_id = #{permissionId},
            </if>
        </set>
        where role_permission_id = #{rolePermissionId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from myproject.t_role_permission where role_permission_id = #{rolePermissionId}
    </delete>

</mapper>